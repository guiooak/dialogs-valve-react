---
"@dialogs-valve/react": patch
---

Add `locationSearch` prop to `DialogsValveProvider` for passing a reactive URL search string from the host router, replacing the built-in location listener when provided.
