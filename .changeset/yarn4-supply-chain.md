---
---

chore: migrate the repo from Yarn 1 to Yarn 4 and move supply-chain hardening into Yarn config.

Tooling/dev-infra only — no change to the published `@dialogs-valve/react` runtime, so this changeset is intentionally empty (no version bump).
