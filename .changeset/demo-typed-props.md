---
---

docs(demo): show number and boolean props in the "Props via URL" demo section.

This touches only the demo app (`dialogs-valve-react-demo-app`, which is `private`), not the published `@dialogs-valve/react` package — so this changeset is intentionally empty (no version bump).
