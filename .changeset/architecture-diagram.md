---
---

docs: add an Excalidraw architecture diagram of the library under `docs/`.

Documentation asset only — it is not part of the published `@dialogs-valve/react` package (`files` ships `dist`, `README`, `LICENSE`), so this changeset is intentionally empty (no version bump).
